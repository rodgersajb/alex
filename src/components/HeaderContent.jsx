// import { headerContent } from "../hooks/HeaderContent";

// import Header from "./Header";

// const HeaderContent = () => {
//   return headerContent.map((header, index) => {
//     return <Header key={index} header={header} />;
//   });
// };

// export default HeaderContent;
